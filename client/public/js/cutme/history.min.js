function initHistoryList(){
  $.get({
    url:`http://localhost:8080/api/orders`,
    success:function(result){
      var history = $("#history-list")
      for(var idx = 0; idx < result.length; idx++){
        var reader = new window.FileReader();
        // console.log(result[idx].imgBefore)
        // console.log(jQuery.parseJSON(result[idx].imgBefore.toString()))
        // reader.readAsDataURL(result[idx].imgBefore.data)
        // reader.onloadend = function(){
        //   base64data = reader.result
        //
        //   console.log(base64data)
        // }
        var gambar = btoa(result[idx].imgBefore.data.data.toString('base64'))
        console.log(gambar);
        var html = `<li class="item"><div class="row"><div class="col-sm-3"><img src="data:image/jpg;base64,${gambar}" width="150" height="150" /></div><div class="col-sm-9"><h3><strong>${result[idx].hairStyleId}</strong></h3><p>Date: ${result[idx].dt_created}</p><p>Barber: ${result[idx].barberId}</p><p>Location: ${result[idx].address}</p></div></div></liv>`
        history.append(html)
      }
    }
  })
}

$(function(){
  initHistoryList()
})
